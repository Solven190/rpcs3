name: Android Release Build

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Cache Submodules
        uses: actions/cache@v4
        with:
          path: .git/modules
          key: submodules-${{ hashFiles('.gitmodules') }}

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache NDK & CMake
        id: cache-ndk
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.ANDROID_HOME }}/ndk/29.0.14206865
            ${{ env.ANDROID_HOME }}/cmake/3.30.5
          key: ndk-cmake-${{ runner.os }}-29.0.14206865-3.30.5

      - name: Install Dependencies
        if: steps.cache-ndk.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y ccache ninja-build
          sdkmanager --install "ndk;29.0.14206865"
          sdkmanager --install "cmake;3.30.5"

      - name: Install Base Dependencies
        if: steps.cache-ndk.outputs.cache-hit == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y ccache ninja-build

      - name: Setup CCache
        uses: hendrikmuhs/ccache-action@v1
        with:
          key: ccache-${{ runner.os }}
          max-size: 2G

      - name: Configure NDK
        run: echo "NDK_DIR=$ANDROID_HOME/ndk/29.0.14206865" >> $GITHUB_ENV

      - name: Configure Local Properties
        run: echo "sdk.dir=$ANDROID_HOME" > local.properties

      - name: Cache Build Artifacts
        uses: actions/cache@v4
        with:
          path: |
            app/build/ffmpeg-android
            app/build/emucorec-core-build
            app/build/emucorec-core
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: build-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '.gitmodules') }}
          restore-keys: |
            build-${{ runner.os }}-

      - name: Generate Signing Key
        run: |
          keytool -genkeypair -v \
            -keystore app/test-release.keystore \
            -alias testkey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Test, OU=Test, O=Test, L=Test, S=Test, C=US"

      - name: Configure Signing
        run: |
          cat >> app/build.gradle.kts << 'EOF'

          android {
              signingConfigs {
                  create("testRelease") {
                      storeFile = file("test-release.keystore")
                      storePassword = "android"
                      keyAlias = "testkey"
                      keyPassword = "android"
                  }
              }
              buildTypes {
                  getByName("release") {
                      signingConfig = signingConfigs.getByName("testRelease")
                  }
              }
          }
          EOF

      - name: Fix Gradle for Linux
        run: |
          chmod +x gradlew
          mkdir -p /c/Program\ Files/Git/bin/
          ln -sf /usr/bin/bash /c/Program\ Files/Git/bin/bash.exe
          echo "systemProp.bash.path=/usr/bin/bash" >> gradle.properties

      - name: Assemble Release
        run: |
          ./gradlew :app:assembleRelease \
            --parallel \
            --build-cache \
            -Dorg.gradle.jvmargs=-Xmx4g \
            -Dorg.gradle.parallel=true \
            -Dorg.gradle.caching=true

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: EmuCoreC-release-apk
          path: app/build/outputs/apk/release/*.apk

      - name: Debug Build Logs
        if: failure()
        run: |
          find . -name "config.log" -exec tail -n 100 {} \;
          find . -name "CMakeError.log" -exec tail -n 100 {} \;