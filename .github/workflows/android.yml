name: Build APK 

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

concurrency:
  group: android-release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    runs-on: windows-latest
    timeout-minutes: 180
    env:
      CCACHE_DIR: ${{ github.workspace }}\.ccache
      CCACHE_MAXSIZE: 2G

    steps:
      - name: Exclude workspace from Windows Defender
        shell: powershell
        run: |
          Add-MpPreference -ExclusionPath "${{ github.workspace }}"
          Add-MpPreference -ExclusionPath "$env:TEMP"
          Set-MpPreference -DisableRealtimeMonitoring $true

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 1

      - name: Cache git submodules
        uses: actions/cache@v4
        with:
          path: .git/modules
          key: submodules-${{ hashFiles('.gitmodules') }}

      - name: Set up JDK 17 and 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: |
            17
            21

      - name: Set up MSVC environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache NDK and CMake
        id: cache-ndk
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.ANDROID_SDK_ROOT }}\ndk\29.0.14206865
            ${{ env.ANDROID_SDK_ROOT }}\cmake\3.30.5
          key: ndk-cmake-29.0.14206865-3.30.5

      - name: Install NDK and CMake
        if: steps.cache-ndk.outputs.cache-hit != 'true'
        shell: cmd
        run: |
          sdkmanager --install "ndk;29.0.14206865"
          sdkmanager --install "cmake;3.30.5"

      - name: Export NDK_DIR for FFmpeg build script
        shell: powershell
        run: |
          $ndkPath = "$env:ANDROID_SDK_ROOT\ndk\29.0.14206865" -replace '\\', '/'
          "NDK_DIR=$ndkPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Cache MinGW-w64 toolchain
        id: cache-mingw
        uses: actions/cache@v4
        with:
          path: ${{ env.LOCALAPPDATA }}\mingw
          key: mingw-nixman-13.2.0-rt_v11-rev1

      - name: Install MinGW-w64 toolchain (GCC 13.2.0)
        if: steps.cache-mingw.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          $url = "https://github.com/niXman/mingw-builds-binaries/releases/download/13.2.0-rt_v11-rev1/x86_64-13.2.0-release-posix-seh-ucrt-rt_v11-rev1.7z"
          $archive = "$env:RUNNER_TEMP\mingw.7z"
          curl.exe -L --fail --retry 3 -o $archive $url
          $target = "$env:LOCALAPPDATA\mingw"
          New-Item -ItemType Directory -Force -Path $target | Out-Null
          & "C:\Program Files\7-Zip\7z.exe" x $archive -o"$target" -y
          if (-not (Test-Path "$target\mingw64\bin\gcc.exe")) {
            throw "gcc.exe not found after extraction - archive layout unexpected"
          }

      - name: Add MinGW to PATH
        shell: powershell
        run: |
          "$env:LOCALAPPDATA\mingw\mingw64\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Verify MinGW GCC version
        shell: powershell
        run: |
          & "$env:LOCALAPPDATA\mingw\mingw64\bin\gcc.exe" --version

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Point CMake's Python finder at the actions/setup-python install
        shell: powershell
        run: |
          # FindPython3 doesn't scan the actions/setup-python install location on Windows,
          # so expose it via Python3_ROOT_DIR/Python_ROOT_DIR and junction it into the
          # conventional Programs\Python\PythonXXX layout that CMake expects.
          $pythonPath = (Get-Command python).Source
          $pythonDir = Split-Path $pythonPath -Parent

          "Python3_ROOT_DIR=$pythonDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "Python_ROOT_DIR=$pythonDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

          $targetDir = "C:\Users\runneradmin\AppData\Local\Programs\Python"
          New-Item -ItemType Directory -Force -Path $targetDir | Out-Null

          $linkPath = "$targetDir\Python312"
          if (Test-Path $linkPath) {
            Remove-Item $linkPath -Recurse -Force
          }
          cmd /c mklink /J "$linkPath" "$pythonDir"

          python --version

      - name: Install ccache
        shell: powershell
        run: choco install ccache -y --no-progress

      - name: Cache ccache directory
        uses: actions/cache@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-core-${{ hashFiles('android/CMakeLists.txt', 'android/configure-core.cmd') }}-${{ github.run_id }}
          restore-keys: |
            ccache-core-${{ hashFiles('android/CMakeLists.txt', 'android/configure-core.cmd') }}-
            ccache-core-

      - name: Configure local.properties
        shell: powershell
        run: |
          $sdkPath = $env:ANDROID_SDK_ROOT -replace '\\', '/'
          "sdk.dir=$sdkPath" | Out-File -Encoding ascii local.properties

      - name: Cache FFmpeg build
        id: cache-ffmpeg
        uses: actions/cache@v4
        with:
          path: app/build/ffmpeg-android
          key: ffmpeg-android-${{ hashFiles('.ci/build-windows-clang.sh') }}

      - name: Cache native core build
        uses: actions/cache@v4
        with:
          path: app/build/emucorec-core-build
          key: core-build-${{ hashFiles('android/CMakeLists.txt', 'android/configure-core.cmd', 'rpcs3/**') }}
          restore-keys: |
            core-build-

      - name: Generate signing key
        shell: powershell
        run: |
          # Ephemeral key regenerated every run - build verification only, not for distribution.
          $keytool = "$env:JAVA_HOME\bin\keytool.exe"
          & $keytool -genkeypair -v `
            -keystore app/test-release.keystore `
            -alias testkey -keyalg RSA -keysize 2048 -validity 10000 `
            -storepass android -keypass android `
            -dname "CN=Test, OU=Test, O=Test, L=Test, S=Test, C=US"

      - name: Apply release signing config
        shell: powershell
        run: |
          $block = @"

          android {
              signingConfigs {
                  create("testRelease") {
                      storeFile = file("test-release.keystore")
                      storePassword = "android"
                      keyAlias = "testkey"
                      keyPassword = "android"
                  }
              }
              buildTypes {
                  getByName("release") {
                      signingConfig = signingConfigs.getByName("testRelease")
                  }
              }
          }
          "@
          Add-Content -Path "app/build.gradle.kts" -Value $block

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Assemble release
        shell: powershell
        env:
          JAVA_HOME: ${{ env.JAVA_HOME_21_X64 }}
          Python3_ROOT_DIR: ${{ env.Python3_ROOT_DIR }}
          Python_ROOT_DIR: ${{ env.Python_ROOT_DIR }}
        run: |
          python --version
          .\gradlew.bat :app:assembleRelease --stacktrace --parallel --build-cache

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: EmuCoreC-release-apk
          path: app/build/outputs/apk/release/*.apk

      - name: Dump FFmpeg config.log
        if: always()
        shell: powershell
        run: |
          $log = Get-ChildItem -Path . -Recurse -Filter "config.log" -ErrorAction SilentlyContinue | Select-Object -First 1
          if ($log) {
            Write-Host "Found: $($log.FullName)"
            Get-Content $log.FullName -Tail 150
          } else {
            Write-Host "No config.log found anywhere in the workspace."
          }
